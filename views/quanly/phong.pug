extends ./common

block main
  .container.py-5
    h2.text-center THỐNG KÊ PHÒNG

    table.table.table-hover.table-sm
      thead.thead-light
        tr.text-center
          th Mã phòng
          th Giá phòng
          th Số lần được thuê
      tbody
        each p in dsThongKe
          tr.text-center
            td= p.MA_PHONG
            td= p.GIAPHONG
            td= p.SL
block scripts
  if (SCNGP)
    script window.onload = function() {alert('Cập nhật giá thành công')}